<h1>Vos chevaux</h1>

<table class="table table-striped">
    <thead>
        <tr>
            <td>Nom</td>
            <td>ration</td>
            <td>Actions</td>
            
        </tr>
    </thead>
    <tbody>
        <% @horses.each do |horse| %>
            <tr>
                <td><%= horse.name %></td>
                <td> <%= horse.ration %> L  </td>
                
                <td>
                    <%= button_to "Détails", horse_path(horse.id), method: :get, class:"btn" %>
                    <%= button_to "Editer", edit_horse_path(horse.id), method: :get, class:"btn btn-primary" %>
                    <%= button_to "Supprimer", horse_path(horse.id), method: :delete, class:"btn btn-danger" %>
                </td>
                
            </tr>
        <% end %>
    </tbody>
</table>
<p>
<%= button_to "Ajouter", new_horse_path, method: :get, class:"btn btn-success" %>
</p>
