<h1>Performances</h1>

<table class="table table-striped">
    <thead>
        <tr>
            <td>discipline</td>
            <td>cheval</td>
            <td>Actions</td>
            
        </tr>
    </thead>
    <tbody>
       <% @performances.each do |performance| %>
            <tr>
                <td><%= performance.discipline %></td>
                <td>
                    <% if performance.horse %>
                        <p> <%= performance.horse.name %></p>
                    <% end%>
                </td>
                <td>
                    <%= button_to "Détails", performance_path(performance.id), method: :get, class:"btn" %>
                    <%= button_to "Editer", edit_performance_path(performance.id), method: :get, class:"btn btn-primary" %>
                    <%= button_to "Supprimer", performance_path(performance.id), method: :delete, class:"btn btn-danger" %>
                </td>
                
            </tr>
        <% end %>
    </tbody>
</table>
<p>
<%= button_to "Ajouter", new_performance_path, method: :get, class:"btn btn-success" %>
</p>
